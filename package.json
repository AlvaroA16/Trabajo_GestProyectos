{
  "name": "gpweb",
  "version": "1.0.0",
  "private": true,
  "description": "OrientaPerú — Plataforma de orientación vocacional para estudiantes peruanos",
  "scripts": {
    "start": "npm start --prefix frontend",
    "frontend": "npm start --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "dev": "concurrently -n FRONT,BACK -c cyan,green \"npm run frontend\" \"npm run backend\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
